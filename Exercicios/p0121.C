#include <iostream>
#include <cmath>
using namespace std;

float seno_q(float num_int){
    return pow (sin(num_int),2);
}

int main (){
    for (int i=0.4; i=2.1; i=1.5) {
    cout<< i+seno_q(i);
    }
return 0;
}


    // definir uma função z(x)=x +f(x) com f(x)=sin^2(x)
    // os valores para x= 0.4, 2.1 e 1.5